services:
  freellm:
    build: .
    container_name: freellm
    ports:
      - "5180:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
